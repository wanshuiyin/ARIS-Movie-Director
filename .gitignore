# heavy / generated — keep representative panels in examples, ignore bulk
**/_renders/
**/probe/
*.mp4
.DS_Store
__pycache__/
*.pyc
# secrets — never commit
*.env
**/secrets*
# build artifacts (regenerable; the viewer is rebuilt from comic.json + panels)
**/outputs/
# stray / intermediate panel bakes (only the active SXX_panel_aNN.png are tracked)
**/panels/*_baked_text_*.png
**/panels/*_warm_lab_*.png
**/panels/*_cleaned*.png
**/panels/*_textfix*.png
