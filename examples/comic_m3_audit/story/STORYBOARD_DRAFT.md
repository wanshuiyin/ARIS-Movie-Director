# 分镜稿 / STORYBOARD — 「ARIS：我把那 24 小时交出去了 / I Handed Over Those 24 Hours」
storyboard layer draft v1 — revised per codex cross-model critique (verdict REVISE applied); PENDING USER APPROVAL before baking
PAGE ORDER (1/3): P00_cover[S01·full-bleed·cover base] → P01_b02[S02·single·in-panel seam split] → P02_b03[2-up: S03(L warm)|S04(R dark)] → P03_b04[S05·single] → P04_b05[S06·single] → P05_b06[vertical-3: S07 / S08 / S09 big-frame dominant bottom]
PAGE ORDER (2/3): P06_b07[2-up mirror: S10(L)|S11(R)] → P_B08_0[S12a·single·NEW] → P_B08_1[S12·single·reuse] → P_B08_2[S13·single·reuse] → P_B08_3[S14·single·reuse] → P_B08_4[S15·single·reuse] → P02_b08[grid2x2 recap/hero: S12,S13,S14,S15·existing, kept]
PAGE ORDER (3/3): P_B09[S16·single·mirror-of-S11] → P_B09_5[S16b·big-frame·wiki star-map] → P_B10[S17·single] → P_B11[2-up seam: S18(L warm)|S19(R dark)] → P_B12[2-up: S20(L warm)|S21(R seam)] → P03_end[S22·endcard·full-bleed]
TOTALS: 24 panels · 19 pages · 20 NEW bakes (S01–S11, S12a, S16, S16b, S17, S18, S19, S20, S21, S22) + 4 reused DONE images (S12–S15 wide-crop singles; square crops stay in the existing 2×2 recap page — no regeneration)

---

## CORRECTED MOTIF STATE TABLE（codex 表 + 全部 fix 落地）

> 约定：mug 列只追踪她的 `ML RESEARCH` motif 杯；`</>` 杯是**环境道具，不是 motif 杯**（标 env）。curve 拆成两列：`exact_parse`（0.60/0.71/0.66/0.78/0.89）与 `claim_delta`（+6.2/+1.4），二者**永不混写**。DDL 全片单调递减，永不回拨：T-24:00:00 → 19:42 → 19:42 → 18:55 → **18:40** → 18:30 → 17:46 → 17:02 → 16:21 → 16:05 → 05:12 → 02:58 → 02:41 → 01:57 → 00:42 → 00:27(submitted)。bounce 弧线：**S02 是全片唯一 MAX**。

| Panel | Mug（ML RESEARCH motif 杯） | DDL | Tok\|yo | exact_parse | claim_delta | bounce |
|---|---|---|---|---|---|---|
| S01 | **hot，两缕稳定热气（全片温度原点）** | `T-24:00:00` | absent | — | — | n/a（duo 未醒） |
| S02 | **同 S01 热度，不再升高**（origin 在 S01） | `T-24:00:00` | absent | — | — | **MAX（全片唯一最高跳）** |
| S03 | inset 一缕细丝（fading） | `T-19:42`（手机） | absent | — | — | n/a |
| S04 | absent（数字世界） | `T-19:42` chip（与 S03 同刻押韵） | absent | — | — | high（发牌手得意，低于 S02） |
| S05 | absent（env `</>` 杯在控制台，非 motif） | `T-18:55` | absent | — | — | high→tempered（首次被削） |
| S06 | motif 杯不在场（课桌纸杯无热气） | **`T-18:40`**（手机，> S07 的 18:30，倒计时不回拨） | absent | unseen（远端在跑） | — | n/a |
| S07 | env `</>` fresh | `T-18:30` | absent | — | — | **high launch，低于 S02** |
| S08 | env `</>` faint steam | `T-17:46` | absent | **0.60→0.71** | — | high/proud |
| S09 | env `</>` lukewarm | `T-17:02` | **首现：极小角落 chip，低于转录阈值，不 gate** | 0.71 holding | — | **proud peak，低于 S02（pre-fall）** |
| S10 | env `</>` pushed aside, cold | `T-16:21` | **hero 级，钉在正中央（gated）** | **0.71→0.66** | — | deflate（半空冻住） |
| S11 | absent | **`T-16:05`（gated，大字=时间成本）** | 证据缩略图钉在判决卡 | 0.66 flatline | — | SILENT/small（至此最低） |
| S12a | env `</>` cold | `T-05:12` | 极小碎片钉在管线入口（**as prior evidence**，S10/S11 已钉证） | — | **+6.2 held at gate（扣在门口）** | **attempted celebration, ARRESTED before becoming a jump** |
| S12 | as baked（DONE 图锁定，不追溯） | none baked | as baked | — | +6.2（provisional） | arm-up frozen pride（baked；低于 S02） |
| S13 | as baked | none baked | 角落一闪（baked） | — | +6.2 当场失格 | sinking（baked） |
| S14 | as baked | none baked | as baked | — | **+6.2→+1.4 坍缩** | **lowest of the night**（baked） |
| S15 | as baked | none baked | as baked | — | +1.4 · WARN_corrected | first small re-bounce（baked） |
| S16 | env `</>` half-finished | `T-02:58` | gone — `json.loads ✓` 干净 | **0.78→0.89（raw）** | — | tempered micro-bounce |
| S16b | env `</>` nearly empty | `T-02:41` | 化作暗红 REJECT 星 | — | — | small/quiet（settled） |
| S17 | env `</>` empty, pushed aside | `T-01:57` | **REPAIRED：`{"city": "Tokyo"} ✓ parsed`（闭环）** | — | **+1.4 scope-limited 头条；+6.2 划线幽灵** | tempered nod |
| S18 | absent | 不直给（`OPEN 24H` 押 24h 母题；真 DDL 在右格 S19） | absent | — | — | n/a |
| S19 | env `</>` 见底 | **`T-00:42`（红，最后紧迫态）** | absent（S17 已闭环） | 可选极小 `0.89` inset，**不 gate** | （+6.2→+1.4 只进旁白） | small（tempered 小握拳） |
| S20 | **COLD：无一丝热气 + 奶膜（温度计终点，B01 扣合）** | `T-00:27` + `SUBMITTED`（绿稳态） | absent | — | — | n/a（duo 未露脸） |
| S21 | cold 杯一角入画（接 S20） | `SUBMITTED` 顶栏稳态 | absent | — | — | **smallest bounce（弧线终读数）** |
| S22 | implied off-screen（只剩暖窗；B12 拥有冷杯特写） | absent（T-00:42 已投出，倒计时结束） | 一颗无标签暗星 | — | — | n/a |

---

## PER-PANEL SPECS（漫画页序）

### S01 — B01 卧室·倒计时 · full-bleed（兼 P00 封面底图） · world: warm
- text_mode: baked（纯场景 + 小锚蓝图；气泡为零，台词走旁白）
- content_blueprint: 从 `assets/ddl_widget_template_v1.svg` 实例化 `T-24:00:00`（screen-corner 皮肤）—— 深 navy 圆角倒计时挂件：顶部小标签 `DDL`，正文等宽大字 `T-24:00:00`（amber #FFB000 充裕态），立在笔记本旁/屏幕一角（屏幕=暗物，§0.5）。亮黄便签 `dllm-schema-keyword-first` 贴显示器边框，由 scene prompt 携带、**刻意不 gate**（长连字符 token 易烤糊）。
- expected_literals: `["DDL", "T-24:00:00"]`
- scene: 深夜暖光卧室书房（密度=cal_S12）：Edison 台灯下书桌堆满红笔批注的打印论文，书架书脊可读 `LLM DECODING` / `STRUCTURED OUTPUTS` / `EVALS FIRST`，手写 TODO 便签 `better masks ☐ / schema locks ☐ / evals ☑`，笔记本屏幕显示暗色失败实验日志（不画 aris 命令——那是 S02 的），桌角 `ML RESEARCH` 马克杯 **hot：两缕稳定热气（全片温度原点）**，双肩包瘫在椅背，窗外冷蓝夜城天际线与暖室内成单格冷暖对比。
- characters: 研究者她（黑框眼镜下滑+黑短发+黑T，§2）伏案撑头盯着失败日志，精疲力竭；duo **不出现**（S02 才苏醒）。
- bubbles: 无（旁白拍）。
- side_narration:
  - zh: 「离截止还有 24 小时，她在又一轮失败的实验前对自己说：我撑不住了。桌角那杯咖啡正冒着热气——接下来这一夜，将用它的温度来计量。」
  - en: "Twenty-four hours to the deadline, she admits over yet another failed run: I can't hold on. The coffee on her desk is still steaming — the night ahead will be measured in its temperature." *trait: 设场（无能力名；只立赌注与温度计）*
- motifs: mug = `ML RESEARCH` 杯 **hot 两缕稳定热气（温度原点）** · DDL = `T-24:00:00` · Tok|yo = absent · exact_parse = — · claim_delta = — · bounce = n/a
- **封面兼用注**: S01 同时是 P00 封面底图——构图把左侧（comic.json safe_zone main：x0.04 y0.3 w0.55 h0.4）留低细节给 HTML 封面标题，DDL widget/便签等 baked 锚全放**右半**，勿与标题打架。
- asset_requests: `assets/identity/researcher_chibi_canonical_ref_v001.png`（P0）；`assets/ddl_widget_template_v1.svg`；`assets/mug_states_ref_v1.svg`（hot 态）。

### S02 — B02 交接·弹起 (Hand-off) · single (in-panel L→R split warm→dark) · world: seam
- text_mode: baked
- content_blueprint: `assets/handoff_terminal_v1.svg` — "HAND-OFF" 终端卡：大号 prompt 行 `$ aris run --task dllm-schema-keyword-first --deadline 24h`（`$ aris run` 作行首大字）；其下绿色状态戳 `ARIS ONLINE`；右上角 DDL 倒计时 widget 大字 `T-24:00:00`（从 `assets/ddl_widget_template_v1.svg` 实例化，amber #FFB000 数码管风格）。此 SVG 渲染在"缝"上——左侧她的笔记本屏幕即右侧暗世界的入口，终端卡横跨冷暖分界。
- expected_literals: `["T-24:00:00", "ARIS ONLINE", "$ aris run"]`
- scene: 左半 = 暖卧室深夜（Edison 暖灯、`ML RESEARCH` 马克杯**与 S01 同一热度——hot 两缕热气，不再升高**、手写便签 `submit ☐ / sleep ☑`、双肩包靠椅、窗外冷蓝夜城）；她的手指刚离开 Enter 键；画面沿笔记本屏幕呈对角撕开，右半坠入 `#0A0E27` 暗赛博空洞——霓虹网格、wiki 微光、idea 残影——duo 在终端卡下方苏醒，一格之内完成暖→暗双世界交接（§0.5 + §6.5 密度都要拉满）。
- characters: 研究员她（黑框眼镜+黑T+背包带，疲惫但松了口气，按完 Enter 起身）；executor **bounce MAX——全片唯一最高跳**，双臂上挥从暗侧跃起接住任务；reviewer 落地站定、抱臂、目光已锁向终端。
- bubbles:
  - 她（say）: zh 「这一轮交给 ARIS 了，我去睡了。」 / en "This round's on ARIS. I'm going to sleep."
  - executor（shout）: zh 「交给我！」 / en "Leave it to me!"
  - reviewer（terse）: zh 「我盯着。」 / en "I'm watching."
- side_narration:
  - zh: 「23:58，她按下 Enter，把最后 24 小时整个交了出去。ARIS 的自主交接从这一刻生效——此后每一个错误都没有人类兜底，得它自己抓出来。」
  - en: "At 23:58 she hits Enter and hands over the entire final 24 hours. ARIS's autonomous hand-off begins here — from now on no human catches its mistakes; it must catch its own."
- motifs: mug = **同 S01 热度，不再升高**（S01 是温度原点；B12 冷杯的对照起点）· DDL = `T-24:00:00` · Tok|yo = absent · exact_parse = — · claim_delta = — · bounce = **MAX（全片唯一；弧线起点）**
- asset_requests: `assets/handoff_terminal_v1.svg`；`assets/ddl_widget_template_v1.svg`；`assets/mug_states_ref_v1.svg`（hot 态）；`assets/identity/researcher_chibi_canonical_ref_v001.png`；`assets/duo_canonical_ref_v001.png`（既有）。

### S03 — B03 双线·人间侧 (Parallel: her side) · 2-up(L) · world: warm
- text_mode: baked
- content_blueprint: 小锚点（无大技术图）：她课桌上的手机亮屏 lock-screen 卡片——`ARIS RUNNING` 一行 + DDL widget `T-19:42`（`assets/ddl_widget_template_v1.svg` 的 phone-lockscreen 皮肤变体）。锚点小但必须清晰锐利、可盲转录。
- expected_literals: `["ARIS RUNNING", "T-19:42"]`
- scene: 暖色阶梯教室上午光，她在中排记笔记、眼神却飘向手机；课桌上摊开的笔记本写着可读的 `decoding order?` 涂鸦；前景右下嵌一个小暖色 inset 窗格——回到她家书桌：那只 `ML RESEARCH` 马克杯还在原位，**热气已变成一缕细丝**（fading，时间在流逝的环境时钟，§6.5）；教室窗外是白天的城市冷蓝远景，与暖室内成一格内对比。
- characters: 仅研究员她（duo 不在场）：左手压笔记、右手把手机往课桌里推半寸，强迫自己专心。
- bubbles:
  - 她（thought）: zh 「专心上课……专心。」 / en "Focus on class... focus."
- side_narration:
  - zh: 「她回到人间上课，家里那杯咖啡的热气正一点点变淡。ARIS 不占用她的任何一分钟——代价是，接下来的每个岔路口都没人替它拍板。」
  - en: "She returns to the human world for class while the coffee back home slowly loses its steam. ARIS costs her zero minutes — the price is that no one will make its calls at the forks ahead."
- motifs: mug = fading（inset 一缕细丝）· DDL = `T-19:42`（手机锚点）· Tok|yo = absent · exact_parse = — · claim_delta = — · bounce = n/a（duo 不在本格）
- asset_requests: `assets/identity/researcher_chibi_canonical_ref_v001.png`；`assets/ddl_widget_template_v1.svg`（phone-lockscreen 皮肤）；`assets/mug_states_ref_v1.svg`（fading 态）。

### S04 — B03 双线·ARIS 侧 (Parallel: idea fan-out) · 2-up(R) · world: dark-cyber
- text_mode: baked
- content_blueprint: `assets/idea_deck_20_v1.svg` — "IDEA DECK" 扇形卡阵：20 张想法卡从中央生成器喷出呈弧形，顶部大计数徽章 `20 IDEAS`；**仅一张前排卡可读：`#01 schema-first`（gated）；其余卡面——含 #07、#13——一律可见地虚化/降细节为色块**（codex fix：不再有"可读但不 gate"的次级卡，避免盲转录合同冲突）；角落小 DDL chip `T-19:42`（与 S03 同值——两格同刻的押韵）。
- expected_literals: `["20 IDEAS", "#01 schema-first", "T-19:42"]`
- scene: `#0A0E27` 暗空洞里的"想法车间"：霓虹边缘的生成台、飘浮的卡片拖着光尾，暗书脊 `LLM DECODING` / `IDEA SPACE` 立在悬浮架上，一盏暖色小台灯破冷（§0.5 暖点缀），地面霓虹网格向消失点收束（背景大气光允许渐变）。
- characters: executor **bounce high（低于 S02 的 MAX）**——跃在卡阵中央兴奋地把卡扇开，像发牌手；reviewer 站在台边已经开始把卡按某种顺序排队，眉头微皱（为 S05 查新预热）。
- bubbles:
  - executor: zh 「二十个方向，随便挑！」 / en "Twenty directions — take your pick!"
  - reviewer: zh 「先过查新，再说挑。」 / en "Novelty check first. Then pick."
- side_narration:
  - zh: 「同一时刻，executor 在空洞里撒出 20 张想法卡。idea-creator 负责铺开可能性——而可能性铺得越多，等会儿被当面烧掉的也越多。」
  - en: "At the same moment, the executor fans 20 idea cards across the void. idea-creator's job is to spread possibilities — and the more it spreads, the more will be burned in its face soon."
- motifs: mug = absent（数字世界）· DDL = `T-19:42`（与 S03 同值 chip）· Tok|yo = absent · exact_parse = — · claim_delta = — · bounce = **high**（发牌手的得意，低于 S02）
- asset_requests: `assets/idea_deck_20_v1.svg`；`assets/ddl_widget_template_v1.svg`；`assets/duo_canonical_ref_v001.png`（既有）。

### S05 — B04 查新存活 (Novelty gate: 14/20) · single · world: dark-cyber
- text_mode: baked
- content_blueprint: `assets/novelty_gate_14of20_v1.svg` — "NOVELTY GATE" 闸门图：20 张卡排队进入发光闸门；6 张在闸门处烧成红色灰烬、盖 `DUP` 红戳（codex fix：**前景一张大号 `DUP` 红戳卡，大到可盲转录；其余五张 DUP 小号重复**）；其中一张可读小注 `arXiv 2024`（不 gate）；14 张穿过闸门发绿光列队；中央大计分牌 `14/20` + 绿色大印章 `SURVIVES`（#00C896）。`DUP` / `SURVIVES` 均从 `assets/stamp_family_v1.svg` 实例化。角落 DDL chip `T-18:55`。画面里**不出现** skill 名标签（铁律——`novelty-check` 只进侧栏小字）。
- expected_literals: `["14/20", "SURVIVES", "DUP"]`
- scene: 暗赛博"闸门厅"：闸门本体是发光的文献之墙——砖块是密排的暗色论文书脊（`STRUCTURED OUTPUTS` / `PRIOR ART` 两条可读，其余虚化），被烧掉的卡片灰烬上飘起红色像素火星，14 张幸存卡的绿光映在地面霓虹网格上，一只 `</>` 马克杯搁在控制台当暖点缀（**环境道具，非 motif 杯**；§6.5 密度 + §0.5 冷暖破闷）。
- characters: reviewer 在闸门控制台前盖下 `SURVIVES` 印章，沉稳权威；executor **bounce high→tempered**——看着自己的 6 张卡烧掉时肩膀一缩（小型 deflate），看到 14 张绿光列队又稳住半弹。
- bubbles:
  - reviewer: zh 「够新，留下。」 / en "Novel enough. They stay."
  - executor: zh 「我那六张牌啊……」 / en "...my six cards..."
- side_narration:
  - zh: 「20 个想法过查新闸门，6 个当场烧掉——早有人做过了。novelty-check 在动手实验之前先杀重复——代价是 executor 引以为傲的牌，三成没活过这一格。」
  - en: "Twenty ideas hit the novelty gate; six burn on the spot — someone got there first. novelty-check kills duplicates before any experiment runs — the cost: a third of the executor's proud hand never survives this panel."
- motifs: mug = absent（env `</>` 杯属环境，非她的 motif 杯）· DDL = `T-18:55` chip · Tok|yo = absent（要到 B06 实验产出后才首现）· exact_parse = —（首条曲线在 S08）· claim_delta = — · bounce = **high→tempered**（首次被现实削一刀）
- asset_requests: `assets/novelty_gate_14of20_v1.svg`；`assets/stamp_family_v1.svg`（DUP/SURVIVES 实例）；`assets/ddl_widget_template_v1.svg`；`assets/duo_canonical_ref_v001.png`（既有）。

### S06 — B05 教室怀疑 · single · world: warm
- text_mode: baked（场景拍但带手机锚，可 gate）
- content_blueprint: `assets/phone_aris_status_v1.svg` — 手机锁屏状态 widget：顶行 `ARIS · RUNNING`（success_green 圆点 + 等宽字），下行倒计时 **`T-18:40`**（amber；codex fix：原 T-15:27 会让倒计时在 S07 的 T-18:30 处回拨——S06 留在 B05 位置，DDL 改为 18:40 > 18:30，全片倒计时永不回头）；倒计时 chip 字体/布局与 `assets/ddl_widget_template_v1.svg` 共源。整卡贴进课桌上面朝上的手机屏。
- expected_literals: `["ARIS", "RUNNING", "T-18:40"]`
- scene: 午前暖金色阶梯教室（密度=cal_S12）：阳光斜切课桌，黑板写满可读的 attention/去噪公式与示意草图，她的笔记页边**无意识涂鸦了两个小小的蓝/绿 hoodie 火柴人**，桌上纸咖啡杯已无热气（motif 杯不在场——它在家里），手机面朝上亮着 ARIS 状态卡，同学像素剪影虚化，窗外冷色天光与远处教学楼——暖室内 vs 冷窗外。
- characters: 研究者她坐窗边，托腮**望向窗外**，笔尖停在涂鸦上；duo 不出现。
- bubbles:
  - 研究者（thought）: zh 「它真能跑出……能写进论文的东西吗？」 / en "Can it really produce something that belongs in a paper?"
- side_narration:
  - zh: 「第二天的课上，她望向窗外——那 24 小时已经不在她手里了。把研究交出去换来了讲台前的安稳，代价是连怀疑都只能隔着口袋里那块 T-18:40 的屏幕。」
  - en: "In next morning's class she stares out the window — those 24 hours are no longer hers. Handing the research over bought her this seat in a lecture hall; the price is that even her doubt must wait behind a pocket screen reading T-18:40." *trait: 人世喘息拍（自主运行的情感代价：不可介入）*
- motifs: mug = motif 杯不在场（课桌纸杯无热气，呼应 S03 fading）· DDL = **`T-18:40`**（手机）· Tok|yo = absent · exact_parse = unseen（在远端跑）· claim_delta = — · bounce = n/a
- asset_requests: `assets/identity/researcher_chibi_canonical_ref_v001.png`；`assets/phone_aris_status_v1.svg`；`assets/ddl_widget_template_v1.svg`（字体共源）。

### S07 — B06 实验·曲线起飞 (1/3) · single (B06 vertical-3, row 1) · world: dark-cyber
- text_mode: baked
- content_blueprint: `assets/gpu_launch_console_v1.svg` — "GPU LAUNCH CONSOLE"：左上终端块一行 prompt `$ aris exp launch dllm-schema-keyword-first --gpus 8`（小号，非 gate 上下文）；中央 = 8 张像素 GPU 槽卡 2×4 网格，每张带绿色状态灯，顶部大计数 chip `8/8` + 大号绿章 `RUNNING`；右上 = DDL widget chip `T-18:30`（amber #FFB000 on #0A0E27，从 `assets/ddl_widget_template_v1.svg` 实例化）。`8/8` / `RUNNING` / `T-18:30` 三 token 大号高对比；终端行保持次级。
- expected_literals: `["8/8", "RUNNING", "T-18:30"]`
- scene: 夜间暗霓虹机房——8 张发光 GPU 槽卡墙悬于 `#0A0E27` 空洞，地面霓虹网格线，暗书脊 `DIFFUSION LM` / `SCHEDULERS`，副屏滚动分配日志，一只 `</>` 马克杯（环境道具）+ 一盏 Edison 暖台灯破冷（§0.5/§6.5）。
- characters: executor（蓝 hoodie）跃至控制台砸下启动键——**bounce: high launch，低于 S02**（codex fix：S02 是全片唯一 MAX）；reviewer（绿 hoodie）后立抱臂，目光已在日志屏上。
- bubbles:
  - executor: zh 「上 GPU！八张全开！」 / en "To the GPUs — all eight!"
  - reviewer: zh 「日志我盯着。」 / en "I'm watching the logs."
- side_narration:
  - zh: 「她睡着的第一个小时，实验已经真的跑在八张卡上。experiment-bridge 把计划直接变成部署——从这一刻起，出了错也只能由它自己扛。」
  - en: "Within her first hour asleep, the experiment is really running on eight GPUs. experiment-bridge turns the plan straight into deployment — and from this moment, any mistake is ARIS's alone to carry."
- motifs: mug = env `</>` fresh（非 motif 杯）· DDL = `T-18:30`（gated）· Tok|yo = absent · exact_parse = none yet（warm-up）· claim_delta = — · bounce = **high launch，低于 S02**
- asset_requests: `assets/gpu_launch_console_v1.svg`；`assets/ddl_widget_template_v1.svg`；`assets/duo_canonical_ref_v001.png`（既有）。

### S08 — B06 实验·曲线起飞 (2/3) · single (B06 vertical-3, row 2) · world: dark-cyber
- text_mode: baked
- content_blueprint: `assets/wandb_exact_parse_060_071_v1.svg`（从 `assets/wandb_curve_template_v1.svg` 实例化）— wandb 风格图卡，标题 `wandb · exact_parse`：灰色平 baseline 序列标 `random-mask`，终点大刻度 `0.60`；上升绿色序列标 `schema-first`，终点大刻度 `0.71`；两端点间 amber 标注箭头 `0.60 → 0.71`。右上小 DDL widget chip `T-17:46`（非 gate 锚）。两个数字刻度大号、硬边、高对比。
- expected_literals: `["0.60", "0.71"]`
- scene: 同一暗机房收紧到指标墙——大图卡浮于空洞之上，副屏滚动 step 日志 `step 4200…`，暗书脊 `EVALS FIRST` / `MASKING`，便签 `full batch ☐`，`</>` 杯（环境道具）在暖台灯下仍有微弱热气，冷霓虹为底。
- characters: executor 跃上桌面双手指向 `0.71` 刻度——**bounce: high/proud**；reviewer 俯身看日志屏而非曲线，挑一边眉，不为所动。
- bubbles:
  - executor: zh 「0.71！起飞了！」 / en "0.71 — we're flying!"
  - reviewer: zh 「整批跑完再说。」 / en "Wait for the full batch."
- side_narration:
  - zh: 「凌晨的曲线从 0.60 爬到 0.71，executor 蹦得老高。experiment-bridge 实时回传的每个点都只是暂定证据——这份得意，后面每一分都要还。」
  - en: "In the small hours the curve climbs from 0.60 to 0.71, and the executor bounces sky-high. Every point experiment-bridge streams back is provisional evidence — and every bit of this pride will have to be paid back later."
- motifs: mug = env `</>` faint steam · DDL = `T-17:46`（锚，非 gate）· Tok|yo = absent · exact_parse = **0.60→0.71（gated）** · claim_delta = — · bounce = **high/proud**
- asset_requests: `assets/wandb_exact_parse_060_071_v1.svg`（从 `assets/wandb_curve_template_v1.svg` 实例化）；`assets/duo_canonical_ref_v001.png`（既有）。

### S09 — B06 实验·曲线起飞 (3/3，伏笔大格) · big-frame (B06 vertical-3, dominant bottom row) · world: dark-cyber
- text_mode: baked
- content_blueprint: `assets/sample_stream_wall_tokyo_seed_v1.svg` — 大型 "RAW SAMPLE STREAM" 墙：上带 = 上升的 exact_parse 曲线停在大刻度 `0.71`；主体 = 高滚动日志墙，无数 tiny JSON 输出行，几乎全部缀小绿 `OK` chip，顶部大进度计数 `1024/2048`；**流的下角，仅一行渲染坏 chip `{"city":"Tok|yo"}` 带微弱红下划线——刻意极小（≈1–1.5% 画幅高），无 callout、无箭头、不进 expected_literals，并明确标注：低于盲审转录阈值（below blind-review transcription threshold）**；右上小 DDL widget `T-17:02`（非 gate 锚）。复用 canonical `assets/tokyo_fragment_chip_v1.svg` 的 micro 尺度，与 S10 的放大像素级一致。
- expected_literals: `["0.71", "1024/2048"]`（Tok|yo chip 刻意 sub-gate 尺寸；盲审**不要求**看见）
- scene: 暗机房展开成教堂尺度的日志大墙悬于 `#0A0E27` 空洞——霓虹流柱、地面网格反射绿 OK chip 光、暗书脊 `STRUCTURED OUTPUTS` / `RAW LOGS`、带磁盘小图标的归档抽屉，`</>` 杯（环境道具）已温吞、独盏暖灯旁，executor 的彩纸像素暖色飘散在冷墙前。
- characters: executor 前景中央迎着大曲线腾跃、背对流的角落——**bounce: proud peak，低于 S02（pre-fall：跌落前的最高，但不及 S02 的 MAX）**；reviewer 在墙边，半转向下方流——**眯起眼**——只有他朝向那个角落。
- bubbles:
  - executor: zh 「全绿！她醒来会有惊喜！」 / en "All green! She'll wake up to a surprise!"
  - reviewer: **silent, no bubble**（眯眼承担这一拍）。
- side_narration: **NONE** — 用户决策（outline B06：伏笔，无旁白）。无 caption、无旁白；任何文字都不得指向角落碎片。B06 的 trait 句已由 S07/S08 承担。
- motifs: mug = env `</>` lukewarm · DDL = `T-17:02`（锚，非 gate）· Tok|yo = **首现——极小角落 chip，无人提及，低于转录阈值** · exact_parse = 0.71 holding（gated）· claim_delta = — · bounce = **proud peak，低于 S02（pre-fall）**；reviewer 眯眼作反向砝码
- asset_requests: `assets/sample_stream_wall_tokyo_seed_v1.svg`；`assets/tokyo_fragment_chip_v1.svg`（canonical micro chip，与 S10 共享）；`assets/duo_canonical_ref_v001.png`（既有）。

### S10 — B07 REJECT·碎JSON (L) · 2-up(L) · world: dark-cyber
- text_mode: baked
- content_blueprint: `assets/tokyo_parse_error_zoom_v1.svg` — 中央钉死的放大卡：同一 chip `{"city":"Tok|yo"}` 巨幅放大居中（复用 `assets/tokyo_fragment_chip_v1.svg` hero 尺度，红下划线升级为穿过 `|` 的硬 `#FF3366` 裂痕），其下终端行 `json.loads(...)` 以大号红章 `ParseError` 结尾；左缘 = mini wandb 条（`assets/wandb_curve_template_v1.svg` 的 0.71→0.66 dip 实例）：曲线翻落、大红终点刻度 `0.66`、其上小灰幽灵刻度 `0.71`；批次计数 `2048/2048` 小号（非 gate 上下文）；DDL widget `T-16:21` 右上（非 gate 锚）。
- expected_literals: `["Tok|yo", "ParseError", "0.66"]`
- scene: 机房化作审讯室——巨型钉死的 JSON chip 以红边光泛满暗空洞，S09 的 OK chip 光晕在其后黯淡，一根 pinboard 线把 hero chip 视觉连回那行 tiny 角落的缩略图，暗书脊 `JSON SCHEMA` / `EVAL INTEGRITY`，被遗忘的 `</>` 杯（环境道具）推到一边，独盏暖灯成为画面唯一暖像素。
- characters: reviewer 前景一手果断把 chip 钉到屏幕正中央，镇定、胡须紧绷；executor 在他身后被冻在半跳——双臂还半举、表情垮落——**bounce: deflate（半空冻住）**。
- bubbles:
  - reviewer: zh 「这解析不了。」 / en "This won't parse."
  - executor: zh 「只是……个别样本吧？」 / en "Just... a few samples, right?"
- side_narration:
  - zh: 「整批跑完，reviewer 把一条坏 JSON 钉到屏幕正中央——曲线应声跌到 0.66。auto-review-loop 的对抗审不挑时间：它打断的不是外人，是队友刚攒起来的庆祝。」
  - en: "The full batch lands, and the reviewer pins one broken JSON dead-center — the curve sags to 0.66 on cue. auto-review-loop's adversarial review keeps no polite hours: what it interrupts is its own teammate's celebration."
- motifs: mug = env `</>` pushed aside, cold · DDL = `T-16:21`（锚，非 gate）· Tok|yo = **CENTER, hero 尺度（gated）** · exact_parse = **0.71→0.66（0.66 gated）** · claim_delta = — · bounce = **deflate**
- asset_requests: `assets/tokyo_parse_error_zoom_v1.svg`（消费 `assets/tokyo_fragment_chip_v1.svg` hero 尺度）；`assets/wandb_curve_template_v1.svg`（0.71→0.66 dip 实例）；`assets/duo_canonical_ref_v001.png`（既有）。

### S11 — B07 REJECT·碎JSON (R) · 2-up(R) · world: dark-cyber
- text_mode: baked
- content_blueprint: `assets/reject_verdict_round1_v1.svg` — 判决卡砸落居中（与 S16 ACCEPT 卡**同一卡面几何/角度**——两者都从 `assets/stamp_family_v1.svg` 的共享 verdict-card 几何实例化，B07/B09 精确镜像）：表头 `REVIEW · ROUND 1`，正文行 `2048 samples` + 大号红计数 chip `37` 标 `malformed`（`malformed` 小号；`37` 巨幅 #FF3366），Tok|yo chip 缩略图作为证据夹在卡上，对角红章 `REJECT` 统治卡面；DDL widget `T-16:05` 右上，**本格大号渲染**（烧掉的时间=代价）。
- expected_literals: `["REJECT", "37", "T-16:05"]`
- scene: 暗判决厅——盖章的卡在 `#0A0E27` 空洞上泛红光、章击冲击波像素，背后阴影里黯淡的 0.66 曲线与暗书脊 `REVIEW LOG` / `ROUND 1`，地面 loop 箭头浮雕暗示循环重启，暖台灯被推到画面远端，勉强抵着红光。
- characters: reviewer 挥章砸下、平静的权威（与他 S16 盖 ACCEPT 的姿势互为镜像）；executor 缩在下角、肩塌、双臂垂落——**bounce: SILENT/small（至此最低点）**。
- bubbles:
  - reviewer: zh 「打回。修好再来。」 / en "Rejected. Fix it, then come back."
  - executor: zh 「……收到。」 / en "...Copy that."
- side_narration:
  - zh: 「红章砸下：第一轮，打回。auto-review-loop 里两个人不同心是设计——这一夜的进度条，先为诚实倒退了一格。」
  - en: "The red stamp comes down: round one, rejected. In auto-review-loop the two are designed to disagree — and tonight's progress bar steps backward first, to stay honest."
- motifs: mug = absent（没人喝咖啡）· DDL = **`T-16:05`（gated——可见的成本）** · Tok|yo = 证据缩略图归档在判决卡 · exact_parse = 0.66 flatline，背景黯淡 · claim_delta = — · bounce = **SILENT/small**；与 S16 构图镜像锁定
- asset_requests: `assets/reject_verdict_round1_v1.svg`；`assets/stamp_family_v1.svg`（REJECT + 共享 verdict-card 几何）；`assets/duo_canonical_ref_v001.png`（既有）。

### S12a — B08-0 审计入口 / Audit Gate · single（B08 五单页之首） · world: dark-cyber
- text_mode: baked
- content_blueprint: `assets/audit_gate_v1.svg` — 标题条 `EVAL PIPELINE — AUDIT GATE`。横向像素管线：`outputs → evaluator → score`，在 `score` 与 `claim` 之间立着发光闸门/旋转门，盖 `AUDIT` 章。`+6.2` 渲染为亮 amber claim 卡**被实体扣在闸门处**（卡上 `HOLD` chip，不 gate）。大像素放大镜按平在 `evaluator` 块上（镜片扭曲其下方块）。小锚：DDL widget `T-05:12`（角落，不 gate）；管线输入端钉一片极小撕裂的 `Tok|yo` 碎片**作为先前证据（as prior evidence——S10/S11 已钉证，非未解之谜）**，极小、无 callout。
- expected_literals: `["AUDIT", "+6.2", "EVAL PIPELINE"]`
- scene: 深夜屏光审计室，管线全息图后仪表盘一块块亮起；暗书脊 `EVAL INTEGRITY` / `CLAIM AUDIT`，副屏滚动 evaluator 源码，`</>` 杯（环境道具）已凉，霓虹地面网格，独盏暖台灯破冷（cal_S12 密度，单格冷暖对比）。
- characters: executor 的庆祝被半路截停——跳到一半被 reviewer 一手拽住兜帽、彩纸像素抛到一半凝在空中；reviewer 另一手不疾不徐把放大镜按上 evaluator 块。**bounce: attempted celebration, ARRESTED before becoming a jump**（codex fix：不再写 MAX——跌落后的弧线里不允许再出现峰值，这是一次未遂的起跳）。
- bubbles:
  - reviewer: zh 「先过审计门。」 / en "Audit gate first."
  - executor: zh 「现在吗？！」 / en "Now?!"
- side_narration:
  - zh: 「executor 正要庆祝，reviewer 把放大镜按在了评分管线上——最好看的数字，最先受审。ARIS 先审计、后庆祝——代价是 +6.2 被扣在审计门口，无法成为 claim。」
  - en: "The executor was mid-cheer when the reviewer pressed a magnifier onto the evaluation pipeline — the prettiest number gets audited first. ARIS audits before it celebrates; the cost is that +6.2 is held at the gate and cannot become a claim."
- motifs: mug = env `</>` cold, no steam · DDL = `T-05:12` · Tok|yo = 极小碎片钉在管线入口（**as prior evidence**）· exact_parse = — · claim_delta = **+6.2 held at gate（只以被扣的卡存在）** · bounce = **attempted celebration, ARRESTED before becoming a jump**
- asset_requests: `assets/audit_gate_v1.svg`；`assets/tokyo_fragment_chip_v1.svg`（极小碎片）；`assets/ddl_widget_template_v1.svg`；`assets/stamp_family_v1.svg`（AUDIT 章同家族字形）；`assets/duo_canonical_ref_v001.png`（既有）。

### S12 — B08-1 诱人的胜利 · single（DONE 图复用） · world: warm
- status: 图 `panels/S12_panel_a01.png` **DONE — 不重生成**。新增动作：从 grid 格提升为独立页 `P_B08_1`（16:9 源图 wide crop；既有 square crop 保留给 2×2 recap 页）。spec 只含页面与旁白。
- side_narration:
  - zh: 「第一轮就开出漂亮的成绩：schema-keyword-first 领先 +6.2，executor 已经举起了手。但在 ARIS 里证据只是暂定的（experiment-bridge）——这张曲线还没资格变成 claim，庆祝被按下了暂停。」
  - en: "The first pass looked clean: schema-keyword-first led by +6.2 and the executor's arm was already up. In ARIS, evidence is provisional (experiment-bridge) — the curve hasn't earned claim status yet, so the celebration stays on hold."
- motifs（as baked，锁定不追溯）: mug = as baked · DDL = none baked · Tok|yo = as baked · exact_parse = — · claim_delta = +6.2（provisional）· bounce = arm-up frozen pride（baked；低于 S02）

### S13 — B08-2 暗藏的修复 · single（DONE 图复用） · world: dark-cyber
- status: 图 `panels/S13_panel_a01.png` **DONE** — 页 `P_B08_2`，wide crop；square crop 保留给 recap。
- side_narration:
  - zh: 「审计扒开评分管线：评测器在打分之前，偷偷把坏 JSON 修好了。experiment-audit 审的是仪器而不只是输出——代价是，刚刚那 +6.2 当场失去资格。」
  - en: "The audit cut the pipeline open: the evaluator had been silently repairing broken JSON before scoring it. experiment-audit checks the apparatus, not just the outputs — and the cost is that the +6.2 is disqualified on the spot."
- motifs（as baked）: Tok|yo = 角落一闪（baked）· exact_parse = — · claim_delta = +6.2 当场失格 · bounce = sinking（baked）

### S14 — B08-3 裸重评坍缩 · single（DONE 图复用） · world: dark-cyber
- status: 图 `panels/S14_panel_a01.png` **DONE** — 页 `P_B08_3`，wide crop；square crop 保留给 recap。
- side_narration:
  - zh: 「同一批输出绕开修复器裸跑重评，+6.2 当场坍缩成 +1.4，多出来的 +4.8 是评测器自己修出来的。honest re-run 在论文存在之前就纠正了自己的 overclaim——executor 的得意被压到整夜最低点。」
  - en: "The same outputs were rescored raw, bypassing the sanitizer: +6.2 collapsed to +1.4, and the extra +4.8 turned out to be the evaluator's own repairs. The honest re-run corrects its own overclaim before the paper exists — and the executor's pride hits its lowest point of the night."
- motifs（as baked）: exact_parse = — · claim_delta = **+6.2→+1.4 坍缩** · bounce = **lowest of the night**（baked）

### S15 — B08-4 WARN_corrected · single（DONE 图复用） · world: dark-cyber
- status: 图 `panels/S15_panel_a01.png` **DONE** — 页 `P_B08_4`，wide crop；square crop 保留给 recap。**此页之后，保留既有 `P02_b08` grid2x2 `[S12,S13,S14,S15]` 作 recap/hero 页（GitHub 截图单元），原样不动。** B08 口径（全文统一）：**五张单页 = S12a、S12、S13、S14、S15**（+ 2×2 跨页保留为 recap/hero）。
- side_narration:
  - zh: 「判决落下：WARN_corrected——结果没有被删除，而是被降级、标注、带着边界继续往前走。ARIS 保住有用的信号、把不确定写在明面上——代价是，论文里再也写不出 +6.2。」
  - en: "The verdict lands: WARN_corrected — the result isn't deleted, it's downgraded, labeled, and carried forward with its scope. ARIS keeps the useful signal and marks the uncertainty out loud — the cost: +6.2 can never appear in the paper."
- motifs（as baked）: exact_parse = — · claim_delta = +1.4 · WARN_corrected · bounce = first small re-bounce（baked）

### S16 — B09 重跑·ACCEPT（镜像 B07）· single · world: dark-cyber
- text_mode: baked
- content_blueprint: `assets/accept_rerun_v1.svg` — 标题条 `HONEST RE-RUN — RAW SCORING`。wandb 风格 `exact_parse` 曲线上升，两端点都 callout：`0.78`（起点）与 `0.89`（终点）（从 `assets/wandb_curve_template_v1.svg` 实例化）；`RAW` 开关 chip 锁定 ON（呼应 S14 的开关，不 gate）；大绿章 `ACCEPT` 以与 S11 红 REJECT **完全相同的对角几何**砸落（镜像构图，#00C896 辉光；从 `assets/stamp_family_v1.svg` 共享 verdict-card 几何实例化）；干净的单行解析日志 `json.loads ✓`（不 gate）；DDL widget `T-02:58`（角落，不 gate）。
- expected_literals: `["ACCEPT", "0.78", "0.89"]`
- scene: 与 S14 同一暗色重评作战室、但松了一口气——监视器从红 diff 换成稳定的绿解析日志，`RAW` 开关像被守住的承诺一样发光，书脊 `RESULT INTEGRITY` / `EVAL INTEGRITY`，霓虹地面网格，`</>` 杯（环境道具）喝了一半，独盏暖台灯破冷。
- characters: reviewer 用与 S11 砸 REJECT 完全一致的手臂姿势盖下绿 ACCEPT（刻意镜像）；executor 在曲线旁，只允许自己小小一跳。**bounce: tempered micro-bounce（proud but earned）**。
- bubbles:
  - executor: zh 「这次是真的。」 / en "This one's real."
  - reviewer: zh 「裸跑也站得住。」 / en "It holds even raw."
- side_narration:
  - zh: 「绕开修复器重跑，exact_parse 从 0.78 涨到 0.89，这一次绿章 ACCEPT 落下。honest re-run 让 ACCEPT 变得可信——代价是它来得更晚、数字更小，但没有人需要替它脸红。」
  - en: "Rerun with the sanitizer bypassed, exact_parse climbed 0.78 to 0.89, and this time the green ACCEPT lands. The honest re-run is what makes an ACCEPT trustworthy — it arrives later and smaller, but nobody has to blush for it."
- motifs: mug = env `</>` half-finished · DDL = `T-02:58` · Tok|yo = gone — 解析日志干净（`json.loads ✓`）· exact_parse = **0.78→0.89（raw）** · claim_delta = — · bounce = **tempered micro-bounce**
- asset_requests: `assets/accept_rerun_v1.svg`；`assets/stamp_family_v1.svg`（ACCEPT = S11 REJECT 的几何镜像）；`assets/wandb_curve_template_v1.svg`（0.78→0.89 实例）；`assets/duo_canonical_ref_v001.png`（既有）。

### S16b — B09.5 研究星图 / Research Star-Map · big-frame · world: starfield
- text_mode: baked
- content_blueprint: `assets/wiki_starmap_v1.svg` — 小标题 chip `RESEARCH WIKI`（顶角，大号可转录）。像素夜空"研究星图桌"：低矮全息桌的桌面即是夜空图；节点=发光星、细光线相连，整夜主路径按序亮起：`idea → experiment → REJECT → audit → WARN_corrected → ACCEPT → paper`。**字号分级（codex fix）：只有 `RESEARCH WIKI` / `REJECT` / `WARN_corrected` / `ACCEPT` 四个标签渲染为大号可盲转录；其余路径标签（idea / experiment / audit / paper）一律小号、可见但不要求转录、不 gate。** 节点配色：`REJECT` 暗红（#FF3366 低辉光）、`WARN_corrected` amber、`ACCEPT` 绿、`paper` 最亮的白。几颗无标签的暗淡支线星挂在 `idea` 下（查新阵亡者——在场、不亮、无文字）。小锚：DDL widget `T-02:41`（不 gate）。**节点坐标导出为 `assets/wiki_starmap_nodes_v1.json`——本页是带标签的 wiki reveal，B13/S22 是它的无字诗化孪生，坐标文件是两者唯一的真值源。**
- expected_literals: `["RESEARCH WIKI", "REJECT", "WARN_corrected", "ACCEPT"]`
- scene: 暗室远墙溶解为开阔像素夜空；星图桌悬浮在书桌原位，图光映上 duo 的脸，被弃的仪表盘在身后黯淡成余烬，独盏暖台灯仍在画框边缘燃着，像星空下的篝火。
- characters: executor 与 reviewer 并肩立于桌缘，在星空下显得很小，看路径一节节亮起——executor 的手悬停在那颗暗红 REJECT 星上方。**bounce: small/quiet（settled，不是 deflated）**。
- bubbles:
  - executor: zh 「连被拒的那条也在。」 / en "Even the rejected branch is here."
  - reviewer: zh 「失败也是记忆。」 / en "Failure is memory too."
- side_narration:
  - zh: 「黎明前，ARIS 把这一夜写进 research wiki：想法、被否的 claim、审计、判决、论文决策，连成一条发光的路。research-wiki 把失败也存档成记忆——被拒的那颗星不亮，但它在，下一个 agent 不必再撞一次。」
  - en: "Before dawn, ARIS writes the night into the research wiki: ideas, the rejected claim, the audit, the verdict, the paper decision — one glowing path. research-wiki keeps even the failures as memory — the rejected star stays dark, but it stays, so the next agent never has to hit it again."
- interaction (HTML layer, not baked): 本页 click-through chip → `wiki/wiki_trace.html`，文案 zh「打开这一夜的研究记忆」/ en "open the night's memory graph"。
- motifs: mug = env `</>` nearly empty, 搁在桌缘 · DDL = `T-02:41` · Tok|yo = 化作那颗暗红 REJECT 星 · exact_parse = —（图即是 figure）· claim_delta = — · bounce = **small/quiet**
- asset_requests: `assets/wiki_starmap_v1.svg`；`assets/wiki_starmap_nodes_v1.json`（节点坐标导出，S22 复用）；`assets/stamp_family_v1.svg`（红/amber/绿 mini 节点章字形）；`wiki_trace.html` click-chip overlay（viewer HTML 资产）；`assets/duo_canonical_ref_v001.png`（既有）。

### S17 — B10 诚实成稿 / The Honest Paper · single · world: dark-cyber
- text_mode: baked
- content_blueprint: `assets/paper_scope_limited_v1.svg` — 标题条 `PAPER — RESULTS`。发光论文页 mock：结果表头条行 `schema-keyword-first  +1.4 (scope-limited)` amber 高亮；页边一个淡淡的幽灵 `+6.2` 划线（小号，不 gate）；limitations 行 `holds under an external validator`（不 gate，承接 S15 收窄的 claim）。侧边终端窗：`{"city": "Tokyo"} ✓ parsed` —— Tok|yo 的伤口在屏上愈合。**修好的 Tokyo chip 直接 author 在本 SVG 内部（codex fix：不另开 repaired-chip 资产）。** 小锚：DDL widget `T-01:57`（不 gate）。
- expected_literals: `["+1.4", "scope-limited", "Tokyo"]`
- scene: 深夜暗写作间——论文页是房间里最亮的物体，草稿与划掉的打印件像素散落桌面，书脊 `CLAIM AUDIT` / `STRUCTURED OUTPUTS`，S15 的 claim 台账板在身后微光，空的 `</>` 杯（环境道具）推在一边，键盘上方独盏暖台灯破冷。
- characters: reviewer 坐在键盘前敲 limitations 行，平静的权威；executor 倚在桌缘指着终端里修好的 Tokyo，小小的释然微笑。**bounce: tempered（点头，不是跳）**。
- bubbles:
  - reviewer: zh 「照实写，边界也写上。」 / en "Write it as it is — scope included."
  - executor: zh 「Tokyo 修好了。」 / en "Tokyo parses now."
- side_narration:
  - zh: 「论文照实落笔：+1.4，scope-limited，连那个整夜碎着的 Tokyo 也以修好的样子进了结果页。paper-writing 把边界写进结论——代价是数字变小了，但每一个都敢被检查。」
  - en: "The paper goes down honestly: +1.4, scope-limited, and the Tokyo that spent the night in pieces enters the results page repaired. paper-writing puts the boundary inside the conclusion — the number got smaller, but every digit can survive inspection."
- motifs: mug = env `</>` empty, pushed aside · DDL = `T-01:57` · Tok|yo = **REPAIRED — `{"city": "Tokyo"} ✓ parsed`（闭环）** · exact_parse = — · claim_delta = **+1.4 scope-limited 头条；+6.2 划线幽灵** · bounce = **tempered nod**
- asset_requests: `assets/paper_scope_limited_v1.svg`（内含 repaired Tokyo chip）；`assets/ddl_widget_template_v1.svg`；`assets/duo_canonical_ref_v001.png`（既有）。

### S18 — B11 深夜归途 · 2-up(L) · world: warm（与 S19 组成 seam 对页）
- text_mode: baked（无声场景拍 + 霓虹招牌锚）
- content_blueprint: `assets/street_24h_sign_v1.svg` — 便利店霓虹招牌两行 `OPEN` / `24H`（暖黄霓虹管字、像素硬边），挂店门上方——与「24 小时」母题押韵。**codex fix：招牌是本格唯一 salient 文字——画面内其余一切店招/价签/海报文字一律去除或虚化到不可读。**
- expected_literals: `["OPEN", "24H"]`
- scene: 凌晨空街（密度=cal_S12）：暖黄路灯一盏接一盏接力，便利店 `OPEN 24H` 招牌把暖光泼上人行道，自动门内货架灯火通明却无人（货架标签全部虚化无字），斑马线、一只归巢的猫、卷帘门上的像素涂鸦（抽象图形、无文字），远处冷蓝夜城天际线与头顶几颗星——暖灯带 vs 冷夜色的纵深对比（为 B13 星图留一口气）。
- characters: 研究者她背着双肩包独行穿过店前暖光，步子疲惫但松弛，**没看手机**——她不知道此刻正在发生什么；duo 不出现。
- bubbles: 无（无声拍；与右格 S19 的机器之声对切）。
- side_narration:
  - zh: 「凌晨的街上她往家走，不知道论文正在此刻上传。自主提交意味着没有人坐在键盘前——省下她整夜的同时，也把最后 42 分钟的决定权一并交了出去。」
  - en: "She walks home through the small hours, not knowing the paper is uploading right now. Autonomous submission means no one sits at the keyboard — it saved her the night, and it took the final 42 minutes of say out of her hands." *trait: 自主提交（代价：决定权离手）*
- motifs: mug = absent · DDL = 不直给（`OPEN 24H` 押 24h 母题，真倒计时在右格 S19）· Tok|yo = absent · exact_parse = — · claim_delta = — · bounce = n/a
- asset_requests: `assets/identity/researcher_chibi_canonical_ref_v001.png`；`assets/street_24h_sign_v1.svg`。

### S19 — B11 SUBMITTED · 2-up(R) · world: dark-cyber（seam 对页右半）
- text_mode: baked（技术主画面，必须 gate）
- content_blueprint: `assets/submit_terminal_v1.svg` — 提交终端面板：标题条 `SUBMIT`，进度条 100%，文件行 `paper.pdf ✓`，居中绿色印章 `SUBMITTED`（success_green #00C896，**从 `assets/stamp_family_v1.svg` 实例化——与 DUP/SURVIVES/REJECT/ACCEPT/WARN_corrected 同章体家族**），右下角红色 DDL widget `T-00:42`（warning_red #FF3366 最后紧迫态，从 `assets/ddl_widget_template_v1.svg` 实例化）。
- expected_literals: `["SUBMITTED", "T-00:42", "paper.pdf"]`
- scene: 暗赛博提交舱（密度=cal_S12）：dark_navy_void #0A0E27 底上中央提交终端发光、绿章砸落的余晖未散，副屏滚动上传日志，暗书脊 `CLAIM AUDIT` / `RESEARCH WIKI`，地面霓虹网格线，`</>` 马克杯（环境道具）见底，一盏暖台灯破冷（冷赛博+暖点缀）。
- characters: executor 在终端前看进度条走满，**小幅握拳**——**bounce: small（tempered；被 REJECT 和审计磨过、收着的、不敢再蹦最高）**；reviewer 侧后而立，点一次头。
- bubbles:
  - executor: zh 「投出去了。」 / en "It's in."
  - reviewer: zh 「边界也写上了。」 / en "Scope and all."
- side_narration:
  - zh: 「T-00:42，ARIS 把写明了 scope 边界的论文投了出去。它敢替她按下提交，是因为这个数字先被自己人砍过——从 +6.2 砍到 +1.4，所以 executor 的拳头只敢握得很小。」
  - en: "At T-00:42, ARIS submits the paper with its scope limits spelled out. It dares to press submit on her behalf because the number was first cut by its own hand — from +6.2 down to +1.4, which is why the executor's fist pump stays small." *trait: 自主提交 × 诚实成稿（代价：+6.2→+1.4 的缩水换来敢署名）*
- motifs: mug = env `</>` 杯见底（duo 的杯，不是她的温度计）· DDL = **`T-00:42`（红，最后紧迫态）** · Tok|yo = absent（S17 已闭环为 `Tokyo`）· exact_parse = 可选极小 `0.89` inset、**不 gate** · claim_delta = +6.2→+1.4 只进旁白 · bounce = executor **small（tempered 小握拳）**
- asset_requests: `assets/submit_terminal_v1.svg`；`assets/stamp_family_v1.svg`（SUBMITTED 实例）；`assets/ddl_widget_template_v1.svg`；`assets/duo_canonical_ref_v001.png`（既有）。

### S20 — B12 归家·凉咖啡 · 2-up(L) · world: warm
- text_mode: baked（场景拍 + 屏幕状态条锚）
- content_blueprint: 从 `assets/ddl_widget_template_v1.svg` 实例化 SUBMITTED 变体——笔记本屏幕状态条（暗物，只占屏幕一条横带）：绿 tag `SUBMITTED ✓` + 倒计时 `T-00:27`（绿稳态，仍在走但已无关紧迫）；屏幕其余保持低细节暗色——**本格主角是那只凉掉的杯子，不是屏幕**。codex note：gate 只针对 ASCII `SUBMITTED`；`✓` 是装饰性非 ASCII，不进合同。
- expected_literals: `["SUBMITTED", "T-00:27"]`
- scene: 与 S01 **同一房间同一书桌同机位**的暖光房间（密度=cal_S12，set-continuity：拿 S01 成图当附加 condition ref）：门口背包滑落，台灯还亮着，便签（`dllm-schema-keyword-first` 等）与论文堆原封未动，桌上同一只 `ML RESEARCH` 马克杯 **COLD：无一丝热气、表面结了一层奶膜**，笔记本屏幕只亮一条绿状态带，窗外夜城更深更冷——一切如旧，只有温度变了。
- characters: 研究者她推门进来，一手还拎着包带，另一手**握住马克杯，怔住**；duo 不出现（屏幕只亮状态条，S21 才露脸）。
- bubbles:
  - 研究者（whisper）: zh 「……凉了。」 / en "...It's gone cold."
- side_narration:
  - zh: 「她到家，伸手握住那只杯子——咖啡在她不在的 24 小时里彻底凉透了。ARIS 的整夜没有向她要一分钟，账单全记在这杯没人喝的咖啡上。」
  - en: "She gets home and wraps her hand around the mug — the coffee went stone cold in the 24 hours she was away. ARIS's night asked nothing of her; the bill was charged entirely to a coffee no one drank." *trait: 自主整夜运行的弧线兑现（B01 扣合：温度计读数=代价）*
- motifs: mug = **同一只 `ML RESEARCH` 杯·COLD（无热气+奶膜，温度计终点，B01 扣合——本组最硬的连续性约束）** · DDL = `T-00:27` + `SUBMITTED`（绿稳态）· Tok|yo = absent · exact_parse = — · claim_delta = — · bounce = n/a（duo 未露脸）
- asset_requests: `assets/identity/researcher_chibi_canonical_ref_v001.png`；`assets/mug_states_ref_v1.svg`（cold 态）；`assets/ddl_widget_template_v1.svg`（SUBMITTED ✓ 变体）；**S01 成图作 set-continuity condition ref**。

### S21 — B12 隔屏击掌 · 2-up(R) · world: seam（暖房间 + 暗屏幕，单格内 seam）
- text_mode: baked
- content_blueprint: `assets/screen_highfive_frame_v1.svg` — 屏幕画框：顶栏细条 `SUBMITTED ✓`（codex fix：**顶栏字号必须足够大、可真实盲转录——这是 baked 面板，不是氛围字**）、角落 `ARIS` 小标，中央留空给 duo（由 duo_canonical_ref 条件注入），掌心接触点放一个 glow marker 占位（接触辉光属背景大气光，允许平滑渐变，ART_BIBLE §3 校准）。
- expected_literals: `["SUBMITTED", "ARIS"]`
- scene: 同一暖房间贴近屏幕的近景（密度=cal_S12）：暗色屏幕里蓝 executor 与绿 reviewer 并立，executor 抬掌贴向玻璃内侧与她屏外的手掌隔屏相合、接触处一圈柔光，屏幕顶栏字 `SUBMITTED ✓` 与角落 ARIS 标，屏外暖侧入画：她的指尖、桌沿那杯凉咖啡一角、暖台灯光晕——暖人间与暗数字世界在一块玻璃上相接。
- characters: 研究者她俯身抬掌贴屏；executor 屏内抬掌相合——**bounce: smallest bounce（全片最小；S02 唯一 MAX 的镜像落点，弧线终读数）**；reviewer 屏内侧立，颔首。
- bubbles:
  - 研究者: zh 「我回来了。」 / en "I'm home."
  - executor: zh 「欢迎回来。」 / en "Welcome back."
  - reviewer: zh 「去睡吧。」 / en "Get some sleep."
- side_narration:
  - zh: 「隔着一块玻璃，她和 executor 击了一掌；他只是微微一弹。这一下收着的弹跳是被 REJECT 和审计磨出来的——ARIS 交还给她的不是最漂亮的数字，而是敢署她名字的数字。」
  - en: "Through the glass she and the executor share a high-five; he gives only the smallest bounce. That tempered bounce was ground down by REJECT and the audit — what ARIS hands back is not the prettiest number, but one that can carry her name." *trait: 角色弧兑现（bounce 体温计落点：S02 唯一 MAX → S21 smallest）*
- motifs: mug = 凉杯入画一角（接 S20，不抢戏）· DDL = `SUBMITTED ✓` 顶栏稳态 · Tok|yo = absent · exact_parse = — · claim_delta = — · bounce = executor **smallest bounce（弧线终读数）**
- asset_requests: `assets/identity/researcher_chibi_canonical_ref_v001.png`；`assets/duo_canonical_ref_v001.png`（既有）；`assets/screen_highfive_frame_v1.svg`；S01/S20 成图作 set-continuity condition ref。

### S22 — B13 品牌·纯星座 / Pure Constellation · full-bleed endcard · world: starfield
- text_mode: html（pure-scene；**画面内零 baked 文字**——用户决策：无标签；gate = `stray_text_absence`（**对最终渲染图与源 SVG 都跑硬性文字缺席校验**）+ `safezone_present`）
- content_blueprint: `assets/constellation_layout_v1.svg` — `wiki_starmap_v1.svg` 的无字几何孪生：**节点坐标与边一律程序化派生自 `assets/wiki_starmap_nodes_v1.json`（S16b 导出的同一坐标文件——layout 暗合是硬性要求，不许目测重摆）**，渲染为 dark_navy_void 上的纯发光星点。主路径星亮白/绿暖辉光；失败支线星暗淡余烬红、无标签；paper 星（最亮，上空）拖一条细光线垂向画面底缘一扇极小的暖灯卧室窗——全图唯一暖像素，闭环回 B01。无角色、无文字、无 UI。
- expected_literals: —（none；html mode）
- scene: 沉睡城市上空的全幅像素夜空，整夜的工作冷却成一座形状暗合 wiki 图的星座；屋顶剪影，远下方一扇小小的 amber 窗亮着——研究者在那里睡着，paper 星的光丝垂落到它。
- characters: 无（duo 缺席——他们就是星座）。bounce = n/a。
- bubbles: 无。
- side_narration（渲染在 HTML endcard 层，不上图）:
  - zh: 「这一夜烧完，冷却成一座星座：亮的是走通的路，暗的是没走通的，最亮那颗连回卧室的窗。ARIS 留下的不是一个数字，而是一张能被重新走一遍的图——包括那些不亮的部分。」
  - en: "The night burns down into a constellation: the lit stars are the path that held, the dark ones the paths that didn't, and the brightest star reaches back to a bedroom window. What ARIS leaves behind is not a number but a graph you can walk again — dark branches included."
- endcard (HTML layer, not baked): tagline zh「你不在的时候，研究在。」/ en "While you're away, the research carries on." + ARIS logo + 第二个 `wiki_trace.html` click-chip（「打开这一夜的研究记忆 / open the night's memory graph」）。safe_zones: 底部带 `{x:0.08, y:0.74, w:0.84, h:0.20}` 保持星点稀疏给 tagline（按更长的 EN 行定尺寸）。
- motifs: mug = implied off-screen（只剩暖卧室窗——cold，画外；B12 拥有冷杯特写）· DDL = absent（T-00:42 已投出，倒计时结束）· Tok|yo = 一颗无标签暗星 · exact_parse = — · claim_delta = — · bounce = n/a
- asset_requests: `assets/constellation_layout_v1.svg`（从 `assets/wiki_starmap_nodes_v1.json` 程序化派生，禁目测）；endcard HTML 层（tagline zh/en + ARIS logo + wiki_trace chip）。

---

## CONSOLIDATED ASSET_REQUESTS（canonical 名，全文唯一权威；已去重）

> 命名铁律（codex fix #4 落地）：以下 canonical 名是全片唯一合法引用；草稿中出现过的 `ddl_widget_template.svg` / `ddl_widget_T24_v1.svg` / `stamp_pair_template.svg` / `stamp_pair_v1.svg` / `researcher_canonical_ref_v001.png` / `mug_state_pair_v1` / `mug_states_ref.svg` 等变体一律作废、映射到下表。

1. **`assets/ddl_widget_template_v1.svg`** — 参数化像素倒计时 chip（amber #FFB000 / red #FF3366 / green 稳态 + `SUBMITTED ✓` 变体；三皮肤：screen-corner / phone-lockscreen / dashboard-chip）。**全片时间线唯一权威渲染源。** 实例：S01(T-24:00:00)、S02(T-24:00:00)、S03(T-19:42 手机)、S04(T-19:42 chip)、S05(T-18:55)、S06(T-18:40 手机)、S07(T-18:30)、S08(T-17:46)、S09(T-17:02)、S10(T-16:21)、S11(T-16:05 大号)、S12a(T-05:12)、S16(T-02:58)、S16b(T-02:41)、S17(T-01:57)、S19(T-00:42 红)、S20(T-00:27 绿+SUBMITTED)。状态：**NEW（待 author）**。
2. **`assets/stamp_family_v1.svg`** — 单一印章/判决卡家族：`DUP`、`SURVIVES`、`REJECT`、`ACCEPT`、`WARN_corrected`、`SUBMITTED`（+ 红/amber/绿 mini 节点章字形给星图）+ 共享 verdict-card 几何（S11 REJECT ↔ S16 ACCEPT 精确镜像）。需要的面板：S05、S11、S12a(AUDIT 同家族字形)、S16、S16b(mini 字形)、S19。**约束：必须与 S12–S15 已烤图中的 REJECT/ACCEPT/WARN_corrected 章体视觉一致**（全片不许出现两套印章）。状态：**NEW**。
3. **`assets/identity/researcher_chibi_canonical_ref_v001.png`** — 研究者 chibi 身份权威 ref（黑框眼镜+黑/深色短发+黑T+双肩包带，半平滑脸，ART_BIBLE §2）。需要的面板：S01、S02、S03、S06、S18、S20、S21。状态：**NEW（P0 阻塞项——目前只有 duo 有 canonical ref；生成后登记进 comic.json `identity_refs` 并按格注入 condition）**。
4. **`assets/mug_states_ref_v1.svg`** — 她的 `ML RESEARCH` motif 杯三态连续性 ref：**hot（两缕稳定热气）/ fading（一缕细丝）/ cold（无热气+奶膜）**，锁同款杯身配色与字样。**注：`</>` 杯是环境道具，不属于本 ref、不是 motif 杯。** 需要的面板：S01(hot 原点)、S02(hot 同态)、S03(fading inset)、S20(cold 终点)、S21(cold 一角)。状态：**NEW**。
5. **`assets/wandb_curve_template_v1.svg`** — 坐标轴+双序列+终点刻度+delta-callout 模板。命名实例三个：`0.60→0.71`（S08，即 `assets/wandb_exact_parse_060_071_v1.svg`）、`0.71→0.66`（S10 dip 条）、`0.78→0.89`（S16）。状态：**NEW**。
6. **`assets/tokyo_fragment_chip_v1.svg`** — canonical 坏 JSON 碎片 chip `{"city":"Tok|yo"}` 带红裂痕（tokyo_broken snippet）。一个资产、多尺度：S09 micro（≤1.5% 画幅高、低于转录阈值）/ S10 hero（gated）/ S11 证据缩略图 / S12a 极小碎片。**修好的 `Tokyo` chip 不另开资产——直接 author 在 `assets/paper_scope_limited_v1.svg` 内部（S17）。** 状态：**NEW**。
7. **`assets/wiki_starmap_nodes_v1.json`** — 星图节点坐标导出文件，**`assets/wiki_starmap_v1.svg`（S16b）与 `assets/constellation_layout_v1.svg`（S22）共享的唯一真值源**；B13 程序化派生自 S16b 坐标（禁目测），S22 渲染须过硬性 `stray_text_absence` 校验。状态：**NEW**。
8. **逐格内容 SVG（全部 NEW）**：`assets/handoff_terminal_v1.svg`(S02) · `assets/idea_deck_20_v1.svg`(S04) · `assets/novelty_gate_14of20_v1.svg`(S05) · `assets/phone_aris_status_v1.svg`(S06) · `assets/gpu_launch_console_v1.svg`(S07) · `assets/wandb_exact_parse_060_071_v1.svg`(S08，#5 的实例) · `assets/sample_stream_wall_tokyo_seed_v1.svg`(S09) · `assets/tokyo_parse_error_zoom_v1.svg`(S10) · `assets/reject_verdict_round1_v1.svg`(S11) · `assets/audit_gate_v1.svg`(S12a) · `assets/accept_rerun_v1.svg`(S16) · `assets/wiki_starmap_v1.svg`(S16b) · `assets/paper_scope_limited_v1.svg`(S17，内含 repaired Tokyo) · `assets/street_24h_sign_v1.svg`(S18) · `assets/submit_terminal_v1.svg`(S19) · `assets/screen_highfive_frame_v1.svg`(S21) · `assets/constellation_layout_v1.svg`(S22，#7 派生)。
9. **HTML 层资产（不烤进图）**：`wiki_trace.html` click-chip overlay（S16b 页 + S22 endcard，两处；文案「打开这一夜的研究记忆 / open the night's memory graph」）；S22 endcard 层（tagline zh/en + ARIS logo + 底部 safe-zone 带 `{0.08, 0.74, 0.84, 0.20}`）。
10. **既有复用（零新工作量）**：`assets/duo_canonical_ref_v001.png`（所有 duo 在场面板的身份 condition）；`panels/S12_panel_a01.png` / `S13_panel_a01.png` / `S14_panel_a01.png` / `S15_panel_a01.png`（DONE，wide crop 升单页 + square crop 留 recap，**不重生成**）；既有 `P02_b08` grid2x2 recap 页原样保留；S01 成图作 S20/S21 的 set-continuity condition ref（同房间/同书桌/同机位）。
