# Redaction notes (example wiki)

The wiki nodes were copied from the live project and redacted for the public example:
- absolute paths `~/.../aris_comic_v1/` → relative; `~` → `~`.
- KEPT (the value of the example): node types, the full **failure chains**, all `review` conclusions,
  the `decision`/verdict links, `failure_mode` repair patterns — i.e. the multi-agent loop as it really ran.
- No API keys / machine names were present in nodes (verified). Generated prompts summarized where needed.
